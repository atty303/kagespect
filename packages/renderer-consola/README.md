# @kagespect/renderer-consola

Minimal non-TTY renderer for Kagespect using **consola**.

- Maps `LogAppended` and `TaskMessage` to consola levels
- Prints section/task lifecycle lines
- Ignores unknown events safely
